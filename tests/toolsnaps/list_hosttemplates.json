{
  "_meta": {
    "fastmcp": {
      "tags": []
    }
  },
  "annotations": {
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": true,
    "readOnlyHint": true
  },
  "description": "Find host templates (reusable check/notification configurations for hosts) by name. Pass name_filter with a substring to narrow the result. get_allowed_elements_for_container reports which templates a specific container accepts.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "limit": {
        "anyOf": [
          {
            "maximum": 500,
            "minimum": 1,
            "type": "integer"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Maximum rows to return. Defaults to 50, maximum 500."
      },
      "name_filter": {
        "default": "",
        "description": "Substring to search for in the name. Empty returns everything, capped by limit.",
        "type": "string"
      }
    },
    "type": "object"
  },
  "name": "list_hosttemplates",
  "outputSchema": {
    "description": "Rows plus enough context to know whether they are all of them.",
    "properties": {
      "count": {
        "description": "Number of rows in `items`.",
        "type": "integer"
      },
      "hint": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "How to narrow the query when truncated."
      },
      "items": {
        "description": "The rows, at most `limit` of them.",
        "items": {},
        "type": "array"
      },
      "truncated": {
        "description": "True if openITCOCKPIT held more rows than were returned.",
        "type": "boolean"
      }
    },
    "required": [
      "items",
      "count",
      "truncated"
    ],
    "type": "object"
  },
  "title": "Host Templates"
}
