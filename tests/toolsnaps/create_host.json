{
  "_meta": {
    "fastmcp": {
      "tags": []
    }
  },
  "annotations": {
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": true,
    "readOnlyHint": false
  },
  "description": "Create a host in openITCOCKPIT. With agent_pull_port it is also set up for the openITCOCKPIT agent in pull mode, in one call; services are not discovered from the agent, add them separately once it is reachable. container_name defaults to root. hosttemplate_name defaults to the agent template when a port is given and to 'default host' otherwise; it has to be visible from the container - get_allowed_elements_for_container lists which ones are.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "address": {
        "type": "string"
      },
      "agent_pull_https": {
        "default": false,
        "description": "Reach that agent over HTTPS.",
        "type": "boolean"
      },
      "agent_pull_password": {
        "default": "",
        "description": "Password for that user.",
        "type": "string"
      },
      "agent_pull_port": {
        "default": 0,
        "description": "Port the openITCOCKPIT agent answers on, for a host monitored in pull mode (openITCOCKPIT connects to the agent). 0: no agent connection. The usual port is 3333.",
        "maximum": 65535,
        "minimum": 0,
        "type": "integer"
      },
      "agent_pull_user": {
        "default": "",
        "description": "User for the agent's basic auth, if it asks for one.",
        "type": "string"
      },
      "container_name": {
        "default": "",
        "description": "Target container path. Empty means the root container.",
        "type": "string"
      },
      "description": {
        "default": "",
        "description": "Free-text description. Optional.",
        "type": "string"
      },
      "hosttemplate_name": {
        "default": "",
        "type": "string"
      },
      "name": {
        "type": "string"
      }
    },
    "required": [
      "name",
      "address"
    ],
    "type": "object"
  },
  "name": "create_host",
  "outputSchema": {
    "additionalProperties": true,
    "type": "object"
  },
  "title": "Create Host"
}
